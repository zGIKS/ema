# Legacy install method.
# Prefer `uv sync` (see README) which uses pyproject.toml.

fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
numpy>=2.0

# Optional ML deps (used when FR_ENGINE=insightface)
insightface>=0.7.3
onnxruntime>=1.18
opencv-python-headless>=4.10
